# Server
server:
  port: 8081

# Application
spring:
  application:
    name: auth-service

  #Database
  datasource:
    url: jdbc:mysql://localhost:3306/a_auth_db
    username: root
    password: root

  #Gmail
  mail:
    host: smtp.gmail.com
    port: 587
    username: jitendra.pra40@gmail.com
    password: qwkl fdcy dfzk lgtb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  #Jpa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  #redis
  data:
      redis:
        host: localhost
        port: 6379

  security:
    oauth2:
     resourceserver:
      jwt:
        jwk-set-uri: http://localhost:8081/sso-auth-server/.well-known/jwks.json

# Eureka Client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


#Token Expiration time
jwt:
  access:
    expiration: 900000        # 15 minutes in ms
  refresh:
    expiration: 604800000     # 7 days in ms

# Twilio

twilio:
  account-sid: ${TWILIO_ACCOUNT_SID}
  auth-token: ${TWILIO_AUTH_TOKEN}
  phone-number: ${TWILIO_NUMBER}

